// Coding challenge #8: Create a function that will convert from Celsius to Fahrenheit

function celsiusToFarenhite(n) {
    var temp = ((9 * n) / 5) + 32;
    console.log(temp);
}

var res = celsiusToFarenhite(97);